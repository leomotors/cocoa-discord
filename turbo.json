{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build:app": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "lint": {},
    "format": {},
    "docs": {
      "outputs": ["docs-dist/**"]
    },
    "test": {},
    "dev": {
      "cache": false
    }
  }
}
